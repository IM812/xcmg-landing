# XCMG CE - Лендинг для спецтехники

Одностраничный лид-лендинг для компании XCMG CE с формами заявок на КП и ремонт спецтехники.

## 🚀 Технологии

- **Next.js 16** (App Router)
- **React 19**
- **Tailwind CSS 4**
- **TypeScript**
- **Nodemailer** для отправки email

## 📋 Структура страницы

1. **Шапка (Header)** - липкий хедер с логотипом, меню и кнопкой "Заказать звонок"
2. **Баннер** - информационная плашка
3. **Hero секция** - главный заголовок с преимуществами и CTA
4. **Форма КП** - форма для получения коммерческого предложения
5. **Каталог** - 7 категорий техники XCMG с карточками
6. **Секция ремонта** - отдельный блок для заявок на ремонт
7. **Преимущества** - 5 основных преимуществ компании
8. **Контакты** - контактная информация и футер

## ⚙️ Установка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   npm install
   # или
   pnpm install
   ```

3. Создайте файл `.env.local` на основе `.env.example`:
   ```bash
   cp .env.example .env.local
   ```

4. Настройте SMTP переменные в `.env.local`:
   ```env
   SMTP_HOST=smtp.yandex.ru
   SMTP_PORT=465
   SMTP_SECURE=true
   SMTP_USER=your-email@yandex.ru
   SMTP_PASSWORD=your-password
   ```

5. Запустите dev-сервер:
   ```bash
   npm run dev
   # или
   pnpm dev
   ```

6. Откройте [http://localhost:3000](http://localhost:3000)

## 📧 Настройка Email

### Получатели

Все заявки автоматически отправляются на 3 адреса:
- info@xcmg-ce.com
- pe@texbaza.com
- mkn@texbaza.com

Чтобы изменить список получателей, отредактируйте массив `RECIPIENTS` в файле `/app/api/lead/route.ts`.

### Типы заявок

Система обрабатывает 4 типа форм:
- **callback** - заказ обратного звонка
- **quote** - запрос коммерческого предложения
- **price_request** - запрос стоимости по категории
- **repair** - запрос на ремонт техники

### Примеры SMTP настроек

**Gmail:**
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
```
*Примечание: Для Gmail нужно создать пароль приложения на https://myaccount.google.com/apppasswords*

**Yandex:**
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@yandex.ru
SMTP_PASSWORD=your-password
```

**Mail.ru:**
```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@mail.ru
SMTP_PASSWORD=your-password
```

## 🎨 Замена контента

### Логотип и изображения

Все изображения нужно заменить на реальные из Яндекс.Диска:
https://disk.yandex.ru/d/ojxmYRH4nDyDew

**Где заменить:**

1. **Логотип** - в файле `/components/header.tsx` (строка 24)
2. **Фото техники** - в файле `/components/catalog-section.tsx` (массив categories)

### Телефон

Замените телефон в файле `/components/header.tsx` (строка 61):
```tsx
<span>+7 (___) ___-__-__</span>
```

### Цветовая схема

Текущая схема: **серый + желтый** (индустриальный стиль)

Настройки цветов находятся в файле `/app/globals.css`.
Основной акцентный цвет: `--accent: oklch(0.75 0.15 85)` (желтый)

## 🔒 Защита от спама

Реализованы следующие меры:
- **Honeypot поле** - скрытое поле для ловли ботов
- **Валидация на клиенте** - проверка обязательных полей
- **Валидация на сервере** - дополнительная проверка данных
- **Обязательное согласие** - checkbox для согласия на обработку данных

## 📱 Адаптивность

Сайт полностью адаптивен для всех устройств:
- Мобильные телефоны (320px+)
- Планшеты (768px+)
- Десктопы (1024px+)
- Широкие экраны (1280px+)

## 🚀 Деплой

### Vercel (рекомендуется)

1. Загрузите проект на GitHub
2. Импортируйте в Vercel
3. Добавьте переменные окружения SMTP в настройках проекта
4. Деплой произойдет автоматически

### Другие платформы

Проект совместим с любыми платформами, поддерживающими Next.js:
- Netlify
- Railway
- Render
- VPS с Node.js

## 📝 Лицензия

Все права защищены © XCMG CE

---

**Контакты для поддержки:**
- Email: info@xcmg-ce.com
- Расположение: Москва, Россия
