# SMTP Configuration for Email Sending
# Настройте эти переменные для отправки заявок на email

# SMTP Server Host (e.g., smtp.gmail.com, smtp.yandex.ru, smtp.mail.ru)
SMTP_HOST=smtp.example.com

# SMTP Server Port (typically 587 for TLS, 465 for SSL, 25 for non-secure)
SMTP_PORT=587

# Use SSL/TLS (true or false)
SMTP_SECURE=false

# SMTP Username (usually your email address)
SMTP_USER=your-email@example.com

# SMTP Password (your email password or app-specific password)
SMTP_PASSWORD=your-password

# From Email Address (optional, defaults to SMTP_USER)
SMTP_FROM=noreply@xcmg-ce.com

# Example configurations for popular email providers:
#
# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password (create at myaccount.google.com/apppasswords)
#
# Yandex:
# SMTP_HOST=smtp.yandex.ru
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@yandex.ru
# SMTP_PASSWORD=your-password
#
# Mail.ru:
# SMTP_HOST=smtp.mail.ru
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@mail.ru
# SMTP_PASSWORD=your-password
